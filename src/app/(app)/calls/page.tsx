import { ComingSoon } from "@/components/layout/coming-soon";

export default function CallsPage() {
  return (
    <ComingSoon
      title="Call task layer"
      description="AI-recommended and manually created call tasks will appear here once the AI decision layer can flag call_required cases in the next phase."
    />
  );
}
