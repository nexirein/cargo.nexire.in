import { ComingSoon } from "@/components/layout/coming-soon";

export default function RemindersPage() {
  return (
    <ComingSoon
      title="Reminders and no-reply engine"
      description="Scheduled first/final reminders and slip escalation ship alongside the reply-ingestion phase, once there's live reply data to react to."
    />
  );
}
