import { ComingSoon } from "@/components/layout/coming-soon";

export default function TemplatesPage() {
  return (
    <ComingSoon
      title="Template management"
      description="Pre-alert and reply templates currently ship as a single built-in template. A managed template library is planned for a later phase."
    />
  );
}
