// Placeholder. Once a real Supabase project is linked, regenerate with:
//   SUPABASE_PROJECT_REF=<your-project-ref> npm run db:types
// This file is then overwritten with the real generated types and can be
// wired into the Supabase client generics (createClient<Database>(...)).
export {};
